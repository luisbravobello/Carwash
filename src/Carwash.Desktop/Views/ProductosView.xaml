<UserControl x:Class="Carwash.Desktop.Views.ProductosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Background="#FAFBFC">
  <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#FAFBFC">
  <StackPanel Margin="24,8,24,24" Background="#FAFBFC">
    <TextBlock Text="Catálogo" FontSize="26" FontWeight="Bold" Foreground="Black" Margin="0,0,0,16"/>
    <ui:Card Margin="0,0,0,12" Background="White" BorderBrush="#E5E5E5" BorderThickness="1" Padding="20">
      <StackPanel>
        <TextBlock Text="Nuevo producto" FontSize="16" FontWeight="Bold" Foreground="Black"/>
        <StackPanel Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
          <StackPanel>
            <TextBlock Text="Nombre" Foreground="Gray" FontSize="12"/>
            <TextBox Text="{Binding NuevoNombre, UpdateSourceTrigger=PropertyChanged}" Width="220" Padding="8,6" FontSize="14" ToolTip="Nombre nuevo"/>
          </StackPanel>
          <StackPanel Margin="8,0,0,0">
            <TextBlock Text="Precio" Foreground="Gray" FontSize="12"/>
            <TextBox Text="{Binding NuevoPrecio, UpdateSourceTrigger=PropertyChanged}" Width="110" Padding="8,6" FontSize="14" ToolTip="Precio"/>
          </StackPanel>
          <StackPanel Margin="8,0,0,0">
            <TextBlock Text="Stock" Foreground="Gray" FontSize="12"/>
            <TextBox Text="{Binding NuevoStock, UpdateSourceTrigger=PropertyChanged}" Width="90" Padding="8,6" FontSize="14" ToolTip="Stock"/>
          </StackPanel>
          <ui:Button Content="Crear" Command="{Binding CrearCmd}" Appearance="Secondary" Margin="8,22,0,0" Icon="{ui:SymbolIcon Add24}"/>
          <ui:Button Content="Recargar" Command="{Binding RecargarCmd}" Margin="8,22,0,0" Icon="{ui:SymbolIcon ArrowSync24}"/>
        </StackPanel>
      </StackPanel>
    </ui:Card>
    <ui:Card Background="White">
      <StackPanel>
        <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding Seleccionado, Mode=TwoWay}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserSortColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False"
                  IsReadOnly="True" SelectionMode="Single" MinHeight="220">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
            <DataGridTextColumn Header="Precio" Binding="{Binding Precio, StringFormat=N2}" Width="110"/>
            <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80"/>
            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="90"/>
          </DataGrid.Columns>
        </DataGrid>
        <Border Background="#F3F3F3" CornerRadius="8" Padding="12" Margin="0,12,0,0">
          <StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <ui:SymbolIcon Symbol="Edit24" Margin="0,0,8,0"/>
              <TextBlock Text="Editar seleccionado (lápiz)" FontWeight="Bold" Foreground="Black" VerticalAlignment="Center"/>
              <ui:Button Content="Editar" Command="{Binding EditarCmd}" Margin="16,0,0,0" Icon="{ui:SymbolIcon Edit24}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
              <StackPanel>
                <TextBlock Text="Nombre" Foreground="Gray" FontSize="12"/>
                <TextBox Text="{Binding EditNombre, UpdateSourceTrigger=PropertyChanged}" Width="220" Padding="8,6" FontSize="14"/>
              </StackPanel>
              <StackPanel Margin="8,0,0,0">
                <TextBlock Text="Precio" Foreground="Gray" FontSize="12"/>
                <TextBox Text="{Binding EditPrecio, UpdateSourceTrigger=PropertyChanged}" Width="110" Padding="8,6" FontSize="14"/>
              </StackPanel>
              <StackPanel Margin="8,0,0,0">
                <TextBlock Text="Stock" Foreground="Gray" FontSize="12"/>
                <TextBox Text="{Binding EditStock, UpdateSourceTrigger=PropertyChanged}" Width="90" Padding="8,6" FontSize="14"/>
              </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
              <ui:Button Content="Guardar cambios" Command="{Binding GuardarCmd}" Appearance="Secondary" Icon="{ui:SymbolIcon Save24}"/>
              <ui:Button Content="Activar / Desactivar" Command="{Binding ToggleCmd}" Margin="8,0,0,0"/>
            </StackPanel>
          </StackPanel>
        </Border>
        <TextBlock Text="La tabla está bloqueada: selecciona una fila, dale al lápiz y edita en el formulario." Foreground="Gray" FontSize="11" Margin="0,8,0,0"/>
      </StackPanel>
    </ui:Card>
    <TextBlock Text="{Binding Mensaje}" Foreground="Green" Margin="4,8,0,0"/>
  </StackPanel>
  </ScrollViewer>
</UserControl>
